/** Shared chrome plate params — no shader import (keeps this module light). */
export const LIQUID_METAL_CHROME = {
  fit: "cover",
  softness: 0.15,
  speed: 0.8,
  colorBack: "#1a1a1a",
  colorTint: "#f4f4f5",
  shape: "diamond",
  scale: 2.5,
  distortion: 0.1,
} as const;

export const LIQUID_METAL_OPACITY = 0.55;
